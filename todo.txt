backend:
    !fix auth filter whitelist
    !implement refresh token rotation

    rest of the unit and integration tests
    configure CORS and CSRF
    remove I in repository names
    log all requests
    make it so that access tokens dont require database query
    add proper logging (library)
    email confirmation and password reset using real email
    other todos

frontend: 
    port to typescript ?, tests, other todos, toast popups, password reset via email
    fix critical vulnerabilities 

!do leetcode
!docker => docker production, docker development
!study spring boot & other knowledge, swi
!publish on github with documentation
!env file


backend: for some reason sending messages queries database for user after authentication, it should only write
frontend: 
          2) /validate request is sent on logout, it shouldnt
          3) chatroom slice: each view will have id and array of messages, and then when user 
             initially clicks on the room it will fetch the messages and put them in state (webhooks)
          4) responsivity
          5) cleanup function which will delete messages array of each chatview
          6) user should be notified when trying to register with already used nickname and email 

Cloudy review

right now: live reloads 