implement refresh token rotation
remove I in repository names
log all requests
make it so that access tokens dont requre database query
add proper logging (library)
! unit tests and other kind of tests (research) 
configure CORS and CSRF
flyway migrations
logout endpoint

frontend: state management

do leetcode
docker

right now: 
logic that checks wheter or not refresh token is still valid while expired is wrong
unit tests, test cases, spousteni testu kdyz pisu kod
